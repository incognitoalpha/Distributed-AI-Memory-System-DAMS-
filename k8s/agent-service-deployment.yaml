apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-service
  namespace: aimemory
  labels:
    app: agent-service
    tier: orchestration
spec:
  replicas: 2
  selector:
    matchLabels:
      app: agent-service
  template:
    metadata:
      labels:
        app: agent-service
        tier: orchestration
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8086"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: agent-service
          image: aimemory/agent-service:latest
          ports:
            - containerPort: 8086
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "production"
            - name: SERVER_PORT
              value: "8086"
          envFrom:
            - configMapRef:
                name: aimemory-config
            - configMapRef:
                name: aimemory-service-configs
            - secretRef:
                name: aimemory-secrets
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: aimemory
spec:
  selector:
    app: agent-service
  ports:
    - port: 8086
      targetPort: 8086
      name: http
  type: ClusterIP