apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-service
  namespace: aimemory
  labels:
    app: compliance-service
    tier: compliance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: compliance-service
  template:
    metadata:
      labels:
        app: compliance-service
        tier: compliance
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8085"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: compliance-service
          image: aimemory/compliance-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "production"
            - name: SERVER_PORT
              value: "8085"
          envFrom:
            - configMapRef:
                name: aimemory-config
            - secretRef:
                name: aimemory-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: compliance-service
  namespace: aimemory
spec:
  selector:
    app: compliance-service
  ports:
    - port: 8085
      targetPort: 8085
      name: http
  type: ClusterIP