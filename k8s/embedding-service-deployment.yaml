apiVersion: apps/v1
kind: Deployment
metadata:
  name: embedding-service
  namespace: aimemory
  labels:
    app: embedding-service
    tier: core
spec:
  replicas: 2
  selector:
    matchLabels:
      app: embedding-service
  template:
    metadata:
      labels:
        app: embedding-service
        tier: core
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8084"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: embedding-service
          image: aimemory/embedding-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "production"
            - name: SERVER_PORT
              value: "8084"
          envFrom:
            - configMapRef:
                name: aimemory-config
            - secretRef:
                name: aimemory-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: embedding-service
  namespace: aimemory
spec:
  selector:
    app: embedding-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
  type: ClusterIP